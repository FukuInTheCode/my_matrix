{
	"version": "2.0.0",
	"tasks": [
	  {
		"label": "make",
		"type": "shell",
		"command": "make",
		"group": {
		  "kind": "test",
		  "isDefault": true
		},
		"presentation": {
		  "reveal": "always",
		  "panel": "new"
		}
	  },
	  {
		"label": "run",
		"type": "shell",
		"command": "Start-Process powershell -ArgumentList 'Measure-Command { ./out/${input:my_name}.exe ${input:my_args} | Out-Default } | ForEach-Object -Process { Write-Host Running Time: $_.TotalSeconds s}; pause'",
		"group": {
		  "kind": "build",
		  "isDefault": true
		},
		"presentation": {
		  "reveal": "never",
		  "panel": "shared"
		},
		"dependsOrder": "sequence",
		"dependsOn": "make"
	  }
	],

	"inputs": [
		{
			"id": "my_args",
			"description": "What are the arguments for the program?",
			"type": "promptString"
		},
		{
			"id": "my_name",
			"description": "name of the program (without .exe)",
			"type": "promptString"

		}
	],
  }
