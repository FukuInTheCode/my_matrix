#include "../includes/my.h"

int main(int argc, char* argv[])
{
    srand(time(NULL));
    my_matrix_t *arr;

    my_matrix_t *cpy;

    my_matrix_create_array(&arr, "Bite", 3, 1, 1, 2, 2, 3, 3);

    

    return 0;
}
